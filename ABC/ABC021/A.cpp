#include <bits/stdc++.h>

using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int N;
    cin >> N;

    cout << N << '\n';
    for (int i = 0; i < N; i++) cout << 1 << '\n';

    return 0;
}